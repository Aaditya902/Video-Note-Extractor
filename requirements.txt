# Video Note Extractor - Python dependencies
# Python 3.10+ required
#
# Works on:
#   Local machine    : full pipeline (Whisper + FFmpeg + Gemini)
#   Streamlit Cloud  : Gemini-only pipeline (no Whisper/torch)

streamlit>=1.32.0

# ── LLM — Google Gemini (free tier, no credit card) 
# Free: 1,500 req/day · 15/min · 1M token context
# Key:  https://aistudio.google.com
google-genai>=1.0.0

python-dotenv>=1.0.0

# Also requires ffmpeg on system PATH
# Streamlit Cloud: installed via packages.txt
# Local:  brew install ffmpeg  /  sudo apt install ffmpeg
yt-dlp>=2024.1.0

sentence-transformers>=2.7.0   # Local embeddings (~80MB one-time download)
chromadb>=0.4.0                # In-memory vector store

pydantic>=2.0.0

numpy>=1.24.0
tqdm>=4.65.0
rich>=13.0.0

# Streamlit Cloud uses Gemini Audio instead (see transcription/whisper_engine.py)
# Uncomment below for local use with offline Whisper transcription:
#
# openai-whisper>=20231117
# torch>=2.0.0